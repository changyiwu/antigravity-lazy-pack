# 刻意保留平台專屬寫法的檔案，由 cross-device-agent-skills/tools/check-platform.py 讀取。
# 列在這裡的檔案整份跳過檢查（含 BOM），加進來之前先確認是「設計如此」不是「還沒改」。

# 理由同 agents-lazy-guide：讀者是初學者，Windows 預設只有 PowerShell 5.1，
# 無 BOM 的 UTF-8 會被當 ANSI 讀而讓中文爛掉。
scripts/install-skills.ps1
scripts/validate-lazy-pack.ps1
